/** Modelli dati del modulo PIANO-INCL. Da estendere con il codice importato. */
export interface TankInputModel {
  dInt: number;
  lCil: number;
  rho: number;
}

export interface TankResultModel {
  volumeTotale: number;
}
