# CON-TRONCOCON-BOCC (segnaposto)

Cartella e carta predisposte per il serbatoio con coperchio conico e fondo
troncoconico con bocchello. Struttura copiata da `CON-TRONCOCON`: la logica del
bocchello (tronchetto cilindrico di fondo) deve ancora essere implementata.

Stato carta: `draft` (mostrata come "In preparazione"). Portare a `active` in
`assets/data/module.json` solo dopo verifica numerica e typecheck pulito.
