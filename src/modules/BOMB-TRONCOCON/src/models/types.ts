/** Modelli dati del modulo BOMB-TRONCOCON. Da estendere con il codice importato. */
export interface TankInputModel {
  dInt: number;
  lCil: number;
  rho: number;
}

export interface TankResultModel {
  volumeTotale: number;
}
