/** Configurazione comune dell'applicazione (nessuna logica di modulo). */

export const APP_NAME = "TARATURA SERBATOI";

export const APP_DESCRIPTION =
  "Suite modulare per la taratura di serbatoi: un sottoprogramma per ogni configurazione di testate.";

/** Prefisso delle rotte dei moduli. */
export const MODULE_ROUTE_BASE = "/moduli";

/** Pagina dei dati comuni (intestazione report). */
export const COMMON_DATA_ROUTE = "/dati-comuni";

export default { APP_NAME, APP_DESCRIPTION, MODULE_ROUTE_BASE, COMMON_DATA_ROUTE };
